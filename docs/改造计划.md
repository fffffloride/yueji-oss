# 悦己管理端改造计划（悦己DLumière 医美小程序总后台）

> 本计划是总计划的管理端前端部分，阶段编号与服务端计划保持一致。
> 总计划：`E:\CODE\youlai-nest\docs\改造计划.md`
> 节奏：每阶段先等服务端 Swagger 自测通过，再开发管理页面，并录入真实数据供小程序联调。

## 当前进度（2026-09-02）

| 阶段 | 状态 |
|------|------|
| 0 环境连通 | 已完成（`VITE_APP_API_URL=http://localhost:8000`） |
| 2 商品域页面 | 已完成（分类树 + 商品列表/抽屉编辑） |
| 3 订单域页面 | 已完成（列表 + 详情 + 核销 + 导出） |
| 4 会员管理页面 | 已完成 |
| 5 营销基础页面 | 已完成（小程序待接入） |
| 6 预约管理 | 已完成（四类工作台 + 生命周期操作 + 容量日历） |
| 7 页面装修 + 拼团 | 已完成 |
| 8 分销 + 统计 | **8A–8E 已完成** |
| 9 联调与部署 | 未开始 |

阶段 2–5 管理端已落地：

- API：`src/api/product/`
- 页面：`src/views/product/category/index.vue`、`src/views/product/goods/index.vue`
- 菜单由服务端 `sql/mysql/menu_product.sql` 下发（授权 ROOT + ADMIN）
- 金额页面用「元」，提交时转「分」
- 订单：`src/api/order/`、`src/views/order/index.vue`
- 会员：`src/api/member/`、`src/views/member/index.vue`（列表 + 抽屉式 360 视图）
- 营销：`src/api/marketing/`、会员等级、积分规则/流水、优惠券模板/发放/记录页面
- 阶段 5 服务端及 C 端接口已完成，小程序未改
- 预约：`src/api/appointment/`、`src/views/appointment/index.vue`（最简只读记录）

## 一、项目定位

- 本项目基于 youlai `vue3-element-admin` 模板（Vue 3 + Vite + TypeScript + Element Plus + Pinia）。
- 系统层页面（`src/views/system/*`：用户/角色/菜单/字典/配置/日志/通知）保留不动，作为后台权限底座。
- 业务页面全部新增，遵循模板既有分层规范：
  - API 层：`src/api/<域>/index.ts` + `src/api/<域>/types.ts`（参考 `src/api/system/user/`）
  - 页面层：`src/views/<域>/<页面>/index.vue`（参考 `src/views/system/user/index.vue`）
  - 菜单与路由：通过后台「菜单管理」动态配置，不手写静态路由
- 可复用的模板能力：`@wangeditor-next`（富文本）、`exceljs`（导出）、`echarts`（报表）。

## 二、分阶段任务

### 阶段 0：环境连通（对齐总计划阶段 0）

- 确认 `.env.development` 指向本地 youlai-nest 后端（默认 `http://localhost:8000`），`pnpm dev` 启动、admin 登录成功
- 核对模板版本与后端接口兼容性（本模板含 tenant/app 等页面，后端若无对应接口，隐藏相关菜单即可，不删代码）
- 规划业务菜单结构（录入 sys_menu）：商品管理 / 订单管理 / 会员管理 / 营销管理 / 预约管理 / 分销管理 / 数据统计 / 页面管理

### 阶段 2：商品域页面

- `src/api/product/`：分类、SPU、SKU、标签接口封装
- `src/views/product/category/`：三级分类树管理（参考 `src/views/system/dept/` 的树形页面）
- `src/views/product/list/`：商品列表（查询/上下架/标签筛选）
- `src/views/product/edit/`：商品编辑（主图/轮播图上传用模板 file 接口、富文本详情用 wangeditor、多规格 SKU 表格编辑、购买权限设置）

### 阶段 3：订单域页面

- `src/api/order/`：订单查询、详情、核销、导出接口
- `src/views/order/list/`：订单列表（订单号/状态/时间/会员/核销人组合筛选）
- `src/views/order/detail/`：订单详情（商品、支付、核销信息、操作日志）
- 核销操作：核销确认/撤销、批量核销
- 订单导出 Excel（exceljs）

### 阶段 4：会员管理页面（P0 收尾）

- `src/api/member/`：会员查询、详情、标签、备注接口
- `src/views/member/index.vue`：会员列表（昵称/手机号、状态筛选）
- 同页抽屉式会员 360 视图（资料、订单统计、最近订单、标签、备注）

### 阶段 5：营销基础页面（已完成）

- `src/api/marketing/`：等级、积分规则/流水、优惠券模板/发放/记录接口与类型
- `src/views/member/level/`：等级分页、新增编辑、启停和删除，配置累计实付门槛、折扣、排序
- `src/views/marketing/points/`：积分规则配置；按会员、订单、类型和时间筛选积分流水
- `src/views/marketing/coupon/`：满减、折扣、兑换券配置；支持范围、兑换 SKU、库存限领、公开领取、定向发放和领取/使用记录
- `src/views/member/index.vue`：会员 360 增加等级、累计实付和积分入口
- `src/views/order/index.vue`：订单详情增加等级折扣、券优惠和积分抵扣拆分
- 菜单与权限由服务端 `sql/mysql/menu_marketing.sql` 动态下发，授权 ROOT + ADMIN
- 验证：`pnpm type-check`、`pnpm build` 及提交钩子 ESLint/Prettier/Stylelint 均通过
- 小程序未修改，后续直接接入已完成的 C 端营销与订单试算接口

### 阶段 6：预约管理页面（已完成）

- `src/api/appointment/`：预约统计、四类分页、月历、动态时段、详情、改期、取消、面诊完成及容量配置接口
- `src/views/appointment/index.vue`：待预约、待到店、服务记录、已取消四项统计与组合筛选；展示状态、最近变更、订单场景和商品信息
- 详情抽屉展示完整操作时间线；客服可按独立权限代改期、代取消，已开始的面诊预约可人工完成；订单预约仍在订单管理核销
- 月历日角标只统计 `BOOKED` 待到店记录，日详情展示全部预约状态；动作完成后同步刷新统计、列表和月历
- “查看订单”进入订单详情；菜单与操作权限由服务端动态下发，继续复用查询/配置权限并新增改期、取消、完成权限
- 不包含医生、门店、排班、独立号源、爽约、提醒和通知
- 验证：`pnpm type-check`、`pnpm build`，以及改动文件 ESLint/Prettier/Stylelint

### 阶段 7：页面装修 + 拼团

- `src/views/decoration/banner/`：Banner 管理（图片、跳转链接、排序）
- `src/views/decoration/notice/`：公告管理
- `src/views/decoration/brand/`：品牌背书内容编辑（富文本）
- `src/views/marketing/groupbuy/`：拼团活动管理（活动配置、成团要求、拼团订单查看）
- API：`src/api/decoration/`、`src/api/group-buy/`
- 菜单与权限由服务端 `sql/mysql/menu_phase7.sql` 动态下发，授权 ROOT + ADMIN
- 验证：`pnpm type-check`、`pnpm build`；本地页面覆盖成功团、超时退款团和成员详情

### 阶段 8：分销 + 数据统计

- 已完成 `src/api/distribution/` 及代理类型、分销等级、代理商、团队结构、佣金明细页面
- 已完成新增/审核/禁用代理、等级与个人佣金比例调整、二级团队树和佣金状态查询
- 菜单与权限由服务端 `sql/mysql/menu_phase8_distribution.sql` 动态下发，授权 ROOT + ADMIN
- 8C 已完成 `src/views/distribution/settlement/`：结算设置、结算记录、提现审核/驳回和人工打款确认
- 申请/自动模式可切换，周/月/季/年周期和单笔上限可配置，两种模式均必须审核
- 8C 菜单与权限由 `sql/mysql/menu_phase8c_settlement.sql` 动态下发
- 8D 已完成 `src/views/distribution/task/`：任务草稿、发布/取消、全部/等级/代理指派和完成进度查看
- 销售任务只记录核销后直属销售额或订单数的完成状态，不配置奖励
- 8D 菜单与权限由 `sql/mysql/menu_phase8d_distribution_task.sql` 动态下发
- 验证：改动文件 ESLint/Prettier/Stylelint、`pnpm type-check`和生产构建通过
- 8E 已完成 `src/views/distribution/analytics/`：时间筛选、汇总卡、双线趋势、层级统计、代理排名和个人趋势抽屉
- 按当前时间范围导出销售趋势、代理业绩和层级统计三个 Excel 工作表
- 8E 菜单与权限由 `sql/mysql/menu_phase8e_sales_analytics.sql` 动态下发

### 阶段 9：联调与部署

- 全模块权限点核对（按钮级权限走模板 `hasPerm` 指令体系）
- 生产构建 `pnpm build`、部署（可复用 `deploy.mjs`）

## 三、开发约定

- 新页面优先复用既有业务 CRUD 页面模式
- 类型定义与后端 DTO 字段保持一致，统一放 `types.ts`
- 金额展示统一用分转元工具函数；时间统一 `YYYY-MM-DD HH:mm:ss`
- 每阶段完成标志：页面可用 + 权限点配置完成 + 用真实数据录入一遍
