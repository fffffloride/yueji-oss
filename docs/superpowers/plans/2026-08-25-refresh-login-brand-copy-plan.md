# 登录页品牌文案与版本号实施计划

1. 在 `src/views/login/index.vue` 替换左侧品牌标签、标题、描述和三项特质文案。
2. 在 `package.json` 将版本改为 `1.0.0`，沿用 `appConfig.version` 显示版本。
3. 运行 `pnpm type-check` 和 `pnpm build`，检查登录页服务可访问。
4. 提交本次文案与版本号调整。
